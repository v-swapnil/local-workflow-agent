            `Here is some useful information about the environment you are running in:`,
            `<env>`,
            `  Working directory: ${Instance.directory}`,
            `  Workspace root folder: ${Instance.worktree}`,
            `  Is directory a git repo: ${project.vcs === "git" ? "yes" : "no"}`,
            `  Platform: ${process.platform}`,
            `  Today's date: ${new Date().toDateString()}`,
            `</env>`,

                      "Skills provide specialized instructions and workflows for specific tasks.",
          "Use the skill tool to load a skill when a task matches its description.",

                "<available_skills>",
      ...listp
        .sort((a, b) => a.name.localeCompare(b.name))
        .flatMap((skill) => [
          "  <skill>",
          `    <name>${skill.name}</name>`,
          `    <description>${skill.description}</description>`,
          `    <location>${pathToFileURL(skill.location).href}</location>`,
          "  </skill>",
        ]),
      "</available_skills>",

      bash,
apply_patch,
edit,
read,
write,
glob,
grep,
lsp,
question,
skill,
task,
todo,


event stream -> 

I need your help to add 2 more things:

- Capture thinking events in copilot and ollama sessions and show in the event steam (maybe use different color for that)
- Can we somehow combine Tasks and sessions sidebar (This could save up some space):

Session 1:
...
|
|__Task 1
|  ...
|
|__Task 2
|  ...

Session 2:
...
|
|__Task 3:
|  ... 


- Sometime clicking on session with no tasks, shows stale tasks - it should show the empty state
- Improve the UI of task sidebar item (add more details) - we can make it multiline and increase the side of sidebar a bit
- Once task in completed, there's no need to retry it
- handle onUserInputRequest - show a modal similar to tool approve request with details - we can also extend this for ollama with ask_user tool 




Need your help with 2 things:
- Update UserInputModal modal to support input with choices and render a dropdown for that. 
- Improve the Session sidebar UI - alongwith this also refactor the Sessions.tsx file - It has become too big

Use this skill for design/UI updates - https://github.com/anthropics/skills/blob/main/skills/frontend-design/SKILL.md


- Help me out with few changes

- support for worktree in changes page
- 



- Update changes page to support worktree speficic changes
  - we should have a dropdown with list of worktree of the workspace and on selecting a worktree
  - we should see the changes done in that worktree
- We should have a memory system to store relevant details of the session that will be used by tasks 
  - Each tasks should get memory details in prompt and have an option to update memories for the session

